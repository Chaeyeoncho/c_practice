#include <stdio.h>

int main(void) {
    int mm, dd, yyyy ;
    printf("Enter a date(mm/dd/yyyy):  ");
    scanf_s("%d,%d,%d", &mm , &dd , &yyyy);
    printf("You entered the date(mm/dd/yyyy):   ");
    return 0;
}
